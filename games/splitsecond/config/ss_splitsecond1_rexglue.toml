[analysis]
max_jump_extension = 65536
data_region_threshold = 16

# No hints needed: the engine's 16-bit-relative jump tables (e.g. the format-parser
# switch at 0x888C6644, whose out-of-line arms cross data-island region splits) are
# now detected natively by codegen. Previous hints here (0x888C6740 etc.) were just
# arms of that switch.
[functions]
